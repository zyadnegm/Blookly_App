import 'package:flutter/material.dart';

class Custom_CircularProgressIndicator extends StatelessWidget {
  const Custom_CircularProgressIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator(),);
  }
}
